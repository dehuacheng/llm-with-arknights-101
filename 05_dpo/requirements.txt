# Stage 05 DPO — preference optimisation against plausible hallucinations.
# Same toolbox as Stage 04 SFT.
transformers>=4.51.0
peft>=0.11.0
datasets>=2.19.0
accelerate>=0.30.0
huggingface_hub>=1.16.0
# Hand-rolled DPO/IPO loop, but trl is installed for cross-reference
# (and so anyone diffing this against trl.DPOTrainer can do it in-venv).
trl>=0.9.0
